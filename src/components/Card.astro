---
// const title = Astro.props.title;
// const text = Astro.props.text;
// const iconname = Astro.props.inconname
const {title, text, iconname} = Astro.props
import { Icon } from "astro-icon/components";
---


    <div>
        <Icon class="iconcss" name={iconname} />
        <h5>{title}</h5>
        <p>{text}</p>
        <a href="#">Read More →</a>
    </div>



<style>
  div > a {
    color: #181818;
    font-family: Lato;
    font-size: 1.125rem;
    font-style: normal;
    font-weight: 600;
    line-height: 160%; /* 1.8rem */
  }
  div > * {
        margin-bottom: 2rem;
  }
    div {
        background-color: var(--white);
        max-width: 30ch;
        border-radius: 1.25rem;
        margin: 0;
        line-height: 1.5;
        height: fit-content;
        padding: 2em;
        margin-inline: auto;
        font-size: .95rem;
    }   
    .iconcss {
        width: 3.29863rem;
        height: 3.29863rem;
    }

</style>